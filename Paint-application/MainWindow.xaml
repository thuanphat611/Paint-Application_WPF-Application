<Window x:Class="Paint_application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint_application"
        mc:Ignorable="d"
        Title="Paint application" WindowState="Maximized" MinHeight="500" MinWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" MinHeight="80" Background="#24262b" Panel.ZIndex="1">
            <StackPanel.Effect>
                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="6" Opacity="0.5"/>
            </StackPanel.Effect>
        </StackPanel>

        <Grid Grid.Row="1" Background="#A0A0A0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Canvas VerticalAlignment="Center" HorizontalAlignment="Center" Width="1350" Height="620" Margin="50" Background="White">

                </Canvas>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
